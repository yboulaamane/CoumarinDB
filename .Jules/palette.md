## 2024-05-23 - Invalid Button Nesting
**Learning:** Found `<a>` tags nested inside `<button>` tags. This is invalid HTML and poor for accessibility as it confuses screen readers and keyboard navigation.
**Action:** Always refactor to semantic `<a>` tags with button-like styling using CSS classes and `aria-label` attributes.
