## 2026-02-13 - Semantic Download Buttons
**Learning:** Found nested `<button><a>` pattern used for download links. This is invalid HTML and confusing for assistive technologies.
**Action:** Replaced with semantic `<a>` tags using CSS classes for button styling and added `aria-label` for screen reader context.
